/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mercoa.api.resources.entitytypes.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.List;
import java.util.Objects;
import java.util.Optional;

public final class IdentifierList {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private IdentifierList(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static IdentifierList rolesList(List<String> value) {
        return new IdentifierList(new RolesListValue(value));
    }

    public static IdentifierList userList(List<String> value) {
        return new IdentifierList(new UserListValue(value));
    }

    public boolean isRolesList() {
        return value instanceof RolesListValue;
    }

    public boolean isUserList() {
        return value instanceof UserListValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<List<String>> getRolesList() {
        if (isRolesList()) {
            return Optional.of(((RolesListValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<List<String>> getUserList() {
        if (isUserList()) {
            return Optional.of(((UserListValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitRolesList(List<String> rolesList);

        T visitUserList(List<String> userList);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(RolesListValue.class), @JsonSubTypes.Type(UserListValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("rolesList")
    private static final class RolesListValue implements Value {
        @JsonProperty("value")
        private List<String> value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RolesListValue(@JsonProperty("value") List<String> value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRolesList(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RolesListValue && equalTo((RolesListValue) other);
        }

        private boolean equalTo(RolesListValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IdentifierList{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("userList")
    private static final class UserListValue implements Value {
        @JsonProperty("value")
        private List<String> value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserListValue(@JsonProperty("value") List<String> value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUserList(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserListValue && equalTo((UserListValue) other);
        }

        private boolean equalTo(UserListValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IdentifierList{" + "value: " + value + "}";
        }
    }

    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IdentifierList{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
