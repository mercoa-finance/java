/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mercoa.api.resources.customizationtypes.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class PaymentMethodFee {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private PaymentMethodFee(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static PaymentMethodFee percentage(PercentageFee value) {
        return new PaymentMethodFee(new PercentageValue(value));
    }

    public static PaymentMethodFee flat(FlatFee value) {
        return new PaymentMethodFee(new FlatValue(value));
    }

    public static PaymentMethodFee default_(DefaultFee value) {
        return new PaymentMethodFee(new DefaultValue(value));
    }

    public boolean isPercentage() {
        return value instanceof PercentageValue;
    }

    public boolean isFlat() {
        return value instanceof FlatValue;
    }

    public boolean isDefault() {
        return value instanceof DefaultValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<PercentageFee> getPercentage() {
        if (isPercentage()) {
            return Optional.of(((PercentageValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<FlatFee> getFlat() {
        if (isFlat()) {
            return Optional.of(((FlatValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<DefaultFee> getDefault() {
        if (isDefault()) {
            return Optional.of(((DefaultValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitPercentage(PercentageFee percentage);

        T visitFlat(FlatFee flat);

        T visitDefault(DefaultFee default_);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(PercentageValue.class),
        @JsonSubTypes.Type(FlatValue.class),
        @JsonSubTypes.Type(DefaultValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("percentage")
    @JsonIgnoreProperties("type")
    private static final class PercentageValue implements Value {
        @JsonUnwrapped
        private PercentageFee value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PercentageValue() {}

        private PercentageValue(PercentageFee value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPercentage(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PercentageValue && equalTo((PercentageValue) other);
        }

        private boolean equalTo(PercentageValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodFee{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("flat")
    @JsonIgnoreProperties("type")
    private static final class FlatValue implements Value {
        @JsonUnwrapped
        private FlatFee value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private FlatValue() {}

        private FlatValue(FlatFee value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitFlat(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof FlatValue && equalTo((FlatValue) other);
        }

        private boolean equalTo(FlatValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodFee{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("default")
    @JsonIgnoreProperties("type")
    private static final class DefaultValue implements Value {
        @JsonUnwrapped
        private DefaultFee value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DefaultValue() {}

        private DefaultValue(DefaultFee value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDefault(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DefaultValue && equalTo((DefaultValue) other);
        }

        private boolean equalTo(DefaultValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodFee{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "PaymentMethodFee{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
