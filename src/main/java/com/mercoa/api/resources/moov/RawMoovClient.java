/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mercoa.api.resources.moov;

import com.mercoa.api.core.ClientOptions;
import com.mercoa.api.core.MediaTypes;
import com.mercoa.api.core.MercoaApiException;
import com.mercoa.api.core.MercoaException;
import com.mercoa.api.core.MercoaHttpResponse;
import com.mercoa.api.core.ObjectMappers;
import com.mercoa.api.core.RequestOptions;
import com.mercoa.api.resources.moov.requests.MoovAccountCallback;
import com.mercoa.api.resources.moov.requests.MoovBankAccountCallback;
import com.mercoa.api.resources.moov.requests.MoovTransferCallback;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawMoovClient {
    protected final ClientOptions clientOptions;

    public RawMoovClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    public MercoaHttpResponse<Void> moovTransferCallback(MoovTransferCallback request) {
        return moovTransferCallback(request, null);
    }

    public MercoaHttpResponse<Void> moovTransferCallback(MoovTransferCallback request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("internal/webhooks/moov")
                .addPathSegments("transfer")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("X-Account-ID", request.getXAccountId());
        _requestBuilder.addHeader("X-Signature", request.getXSignature());
        _requestBuilder.addHeader("X-Timestamp", request.getXTimestamp());
        _requestBuilder.addHeader("X-Nonce", request.getXNonce());
        _requestBuilder.addHeader("X-Webhook-ID", request.getXWebhookId());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MercoaHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new MercoaApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MercoaException("Network error executing HTTP request", e);
        }
    }

    public MercoaHttpResponse<Void> moovAccountCallback(MoovAccountCallback request) {
        return moovAccountCallback(request, null);
    }

    public MercoaHttpResponse<Void> moovAccountCallback(MoovAccountCallback request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("internal/webhooks/moov")
                .addPathSegments("account")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("X-Account-ID", request.getXAccountId());
        _requestBuilder.addHeader("X-Signature", request.getXSignature());
        _requestBuilder.addHeader("X-Timestamp", request.getXTimestamp());
        _requestBuilder.addHeader("X-Nonce", request.getXNonce());
        _requestBuilder.addHeader("X-Webhook-ID", request.getXWebhookId());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MercoaHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new MercoaApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MercoaException("Network error executing HTTP request", e);
        }
    }

    public MercoaHttpResponse<Void> moovBankAccountCallback(MoovBankAccountCallback request) {
        return moovBankAccountCallback(request, null);
    }

    public MercoaHttpResponse<Void> moovBankAccountCallback(
            MoovBankAccountCallback request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("internal/webhooks/moov")
                .addPathSegments("bankAccount")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
        _requestBuilder.addHeader("X-Account-ID", request.getXAccountId());
        _requestBuilder.addHeader("X-Signature", request.getXSignature());
        _requestBuilder.addHeader("X-Timestamp", request.getXTimestamp());
        _requestBuilder.addHeader("X-Nonce", request.getXNonce());
        _requestBuilder.addHeader("X-Webhook-ID", request.getXWebhookId());
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MercoaHttpResponse<>(null, response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            throw new MercoaApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MercoaException("Network error executing HTTP request", e);
        }
    }
}
