/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mercoa.api.resources.transaction.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class TransactionResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private TransactionResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static TransactionResponse bankAccountToBankAccount(TransactionResponseBankToBankWithInvoices value) {
        return new TransactionResponse(new BankAccountToBankAccountValue(value));
    }

    public static TransactionResponse bankAccountToMailedCheck(TransactionResponseBankToMailedCheckWithInvoices value) {
        return new TransactionResponse(new BankAccountToMailedCheckValue(value));
    }

    public static TransactionResponse bankAccountToPrintedCheck(
            TransactionResponseBankToPrintedCheckWithInvoices value) {
        return new TransactionResponse(new BankAccountToPrintedCheckValue(value));
    }

    public static TransactionResponse bankAccountToWallet(TransactionResponseBankToWalletWithInvoices value) {
        return new TransactionResponse(new BankAccountToWalletValue(value));
    }

    public static TransactionResponse cardToBankAccount(TransactionResponseCardToBankAccountWithInvoices value) {
        return new TransactionResponse(new CardToBankAccountValue(value));
    }

    public static TransactionResponse cardToWallet(TransactionResponseCardToWalletWithInvoices value) {
        return new TransactionResponse(new CardToWalletValue(value));
    }

    public static TransactionResponse walletToBankAccount(TransactionResponseWalletToBankWithInvoices value) {
        return new TransactionResponse(new WalletToBankAccountValue(value));
    }

    public static TransactionResponse walletToWallet(TransactionResponseWalletToWalletWithInvoices value) {
        return new TransactionResponse(new WalletToWalletValue(value));
    }

    public static TransactionResponse custom(TransactionResponseCustomWithInvoices value) {
        return new TransactionResponse(new CustomValue(value));
    }

    public static TransactionResponse offPlatform(TransactionResponseCustomWithInvoices value) {
        return new TransactionResponse(new OffPlatformValue(value));
    }

    public boolean isBankAccountToBankAccount() {
        return value instanceof BankAccountToBankAccountValue;
    }

    public boolean isBankAccountToMailedCheck() {
        return value instanceof BankAccountToMailedCheckValue;
    }

    public boolean isBankAccountToPrintedCheck() {
        return value instanceof BankAccountToPrintedCheckValue;
    }

    public boolean isBankAccountToWallet() {
        return value instanceof BankAccountToWalletValue;
    }

    public boolean isCardToBankAccount() {
        return value instanceof CardToBankAccountValue;
    }

    public boolean isCardToWallet() {
        return value instanceof CardToWalletValue;
    }

    public boolean isWalletToBankAccount() {
        return value instanceof WalletToBankAccountValue;
    }

    public boolean isWalletToWallet() {
        return value instanceof WalletToWalletValue;
    }

    public boolean isCustom() {
        return value instanceof CustomValue;
    }

    public boolean isOffPlatform() {
        return value instanceof OffPlatformValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TransactionResponseBankToBankWithInvoices> getBankAccountToBankAccount() {
        if (isBankAccountToBankAccount()) {
            return Optional.of(((BankAccountToBankAccountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseBankToMailedCheckWithInvoices> getBankAccountToMailedCheck() {
        if (isBankAccountToMailedCheck()) {
            return Optional.of(((BankAccountToMailedCheckValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseBankToPrintedCheckWithInvoices> getBankAccountToPrintedCheck() {
        if (isBankAccountToPrintedCheck()) {
            return Optional.of(((BankAccountToPrintedCheckValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseBankToWalletWithInvoices> getBankAccountToWallet() {
        if (isBankAccountToWallet()) {
            return Optional.of(((BankAccountToWalletValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseCardToBankAccountWithInvoices> getCardToBankAccount() {
        if (isCardToBankAccount()) {
            return Optional.of(((CardToBankAccountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseCardToWalletWithInvoices> getCardToWallet() {
        if (isCardToWallet()) {
            return Optional.of(((CardToWalletValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseWalletToBankWithInvoices> getWalletToBankAccount() {
        if (isWalletToBankAccount()) {
            return Optional.of(((WalletToBankAccountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseWalletToWalletWithInvoices> getWalletToWallet() {
        if (isWalletToWallet()) {
            return Optional.of(((WalletToWalletValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseCustomWithInvoices> getCustom() {
        if (isCustom()) {
            return Optional.of(((CustomValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TransactionResponseCustomWithInvoices> getOffPlatform() {
        if (isOffPlatform()) {
            return Optional.of(((OffPlatformValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitBankAccountToBankAccount(TransactionResponseBankToBankWithInvoices bankAccountToBankAccount);

        T visitBankAccountToMailedCheck(TransactionResponseBankToMailedCheckWithInvoices bankAccountToMailedCheck);

        T visitBankAccountToPrintedCheck(TransactionResponseBankToPrintedCheckWithInvoices bankAccountToPrintedCheck);

        T visitBankAccountToWallet(TransactionResponseBankToWalletWithInvoices bankAccountToWallet);

        T visitCardToBankAccount(TransactionResponseCardToBankAccountWithInvoices cardToBankAccount);

        T visitCardToWallet(TransactionResponseCardToWalletWithInvoices cardToWallet);

        T visitWalletToBankAccount(TransactionResponseWalletToBankWithInvoices walletToBankAccount);

        T visitWalletToWallet(TransactionResponseWalletToWalletWithInvoices walletToWallet);

        T visitCustom(TransactionResponseCustomWithInvoices custom);

        T visitOffPlatform(TransactionResponseCustomWithInvoices offPlatform);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(BankAccountToBankAccountValue.class),
        @JsonSubTypes.Type(BankAccountToMailedCheckValue.class),
        @JsonSubTypes.Type(BankAccountToPrintedCheckValue.class),
        @JsonSubTypes.Type(BankAccountToWalletValue.class),
        @JsonSubTypes.Type(CardToBankAccountValue.class),
        @JsonSubTypes.Type(CardToWalletValue.class),
        @JsonSubTypes.Type(WalletToBankAccountValue.class),
        @JsonSubTypes.Type(WalletToWalletValue.class),
        @JsonSubTypes.Type(CustomValue.class),
        @JsonSubTypes.Type(OffPlatformValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("bankAccountToBankAccount")
    @JsonIgnoreProperties("type")
    private static final class BankAccountToBankAccountValue implements Value {
        @JsonUnwrapped
        private TransactionResponseBankToBankWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BankAccountToBankAccountValue() {}

        private BankAccountToBankAccountValue(TransactionResponseBankToBankWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBankAccountToBankAccount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BankAccountToBankAccountValue && equalTo((BankAccountToBankAccountValue) other);
        }

        private boolean equalTo(BankAccountToBankAccountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("bankAccountToMailedCheck")
    @JsonIgnoreProperties("type")
    private static final class BankAccountToMailedCheckValue implements Value {
        @JsonUnwrapped
        private TransactionResponseBankToMailedCheckWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BankAccountToMailedCheckValue() {}

        private BankAccountToMailedCheckValue(TransactionResponseBankToMailedCheckWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBankAccountToMailedCheck(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BankAccountToMailedCheckValue && equalTo((BankAccountToMailedCheckValue) other);
        }

        private boolean equalTo(BankAccountToMailedCheckValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("bankAccountToPrintedCheck")
    @JsonIgnoreProperties("type")
    private static final class BankAccountToPrintedCheckValue implements Value {
        @JsonUnwrapped
        private TransactionResponseBankToPrintedCheckWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BankAccountToPrintedCheckValue() {}

        private BankAccountToPrintedCheckValue(TransactionResponseBankToPrintedCheckWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBankAccountToPrintedCheck(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BankAccountToPrintedCheckValue && equalTo((BankAccountToPrintedCheckValue) other);
        }

        private boolean equalTo(BankAccountToPrintedCheckValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("bankAccountToWallet")
    @JsonIgnoreProperties("type")
    private static final class BankAccountToWalletValue implements Value {
        @JsonUnwrapped
        private TransactionResponseBankToWalletWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BankAccountToWalletValue() {}

        private BankAccountToWalletValue(TransactionResponseBankToWalletWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBankAccountToWallet(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BankAccountToWalletValue && equalTo((BankAccountToWalletValue) other);
        }

        private boolean equalTo(BankAccountToWalletValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("cardToBankAccount")
    @JsonIgnoreProperties("type")
    private static final class CardToBankAccountValue implements Value {
        @JsonUnwrapped
        private TransactionResponseCardToBankAccountWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CardToBankAccountValue() {}

        private CardToBankAccountValue(TransactionResponseCardToBankAccountWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCardToBankAccount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CardToBankAccountValue && equalTo((CardToBankAccountValue) other);
        }

        private boolean equalTo(CardToBankAccountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("cardToWallet")
    @JsonIgnoreProperties("type")
    private static final class CardToWalletValue implements Value {
        @JsonUnwrapped
        private TransactionResponseCardToWalletWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CardToWalletValue() {}

        private CardToWalletValue(TransactionResponseCardToWalletWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCardToWallet(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CardToWalletValue && equalTo((CardToWalletValue) other);
        }

        private boolean equalTo(CardToWalletValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("walletToBankAccount")
    @JsonIgnoreProperties("type")
    private static final class WalletToBankAccountValue implements Value {
        @JsonUnwrapped
        private TransactionResponseWalletToBankWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private WalletToBankAccountValue() {}

        private WalletToBankAccountValue(TransactionResponseWalletToBankWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitWalletToBankAccount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof WalletToBankAccountValue && equalTo((WalletToBankAccountValue) other);
        }

        private boolean equalTo(WalletToBankAccountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("walletToWallet")
    @JsonIgnoreProperties("type")
    private static final class WalletToWalletValue implements Value {
        @JsonUnwrapped
        private TransactionResponseWalletToWalletWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private WalletToWalletValue() {}

        private WalletToWalletValue(TransactionResponseWalletToWalletWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitWalletToWallet(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof WalletToWalletValue && equalTo((WalletToWalletValue) other);
        }

        private boolean equalTo(WalletToWalletValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("custom")
    @JsonIgnoreProperties("type")
    private static final class CustomValue implements Value {
        @JsonUnwrapped
        private TransactionResponseCustomWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CustomValue() {}

        private CustomValue(TransactionResponseCustomWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCustom(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CustomValue && equalTo((CustomValue) other);
        }

        private boolean equalTo(CustomValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("offPlatform")
    @JsonIgnoreProperties("type")
    private static final class OffPlatformValue implements Value {
        @JsonUnwrapped
        private TransactionResponseCustomWithInvoices value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private OffPlatformValue() {}

        private OffPlatformValue(TransactionResponseCustomWithInvoices value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitOffPlatform(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof OffPlatformValue && equalTo((OffPlatformValue) other);
        }

        private boolean equalTo(OffPlatformValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "TransactionResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
